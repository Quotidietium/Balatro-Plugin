# Balatro v0.4.31 — Showdown Boss 调度与倍率对齐真版（第 56 处修复族）

> 基线：Minecraft 1.21.11 / Paper API（Papo 服务端）。
> R126 真版核对战役第五域（Boss 28）：效果语义 25/28 一致，发现 3 处不符并修复。

## 真版规则 vs 缺陷（Boss_Blinds/The Needle/Violet Vessel Wiki）

1. **Showdown 调度**：5 终结者（琥珀橡子/翠绿之叶/绯红之心/青蓝之铃/紫罗兰之瓶）**仅在
   底注 8/16…出现**；底注 1~7 只从 23 个常规 Boss 抽取。实现为 28 混抽（REF 同 bug；
   R45 的「均匀分布验证」验证的是错误分布——统计断言只证"随机"不证"正确"）。
2. **紫罗兰之瓶 = 6× 基础分**（"6x base"，奖励 $8）；实现为 3×（REF bug）。
3. **缝衣针 = 1× 基础分**（"Play only 1 hand" 的 1x base——一手换低目标）；实现沿用 Boss
   默认 2×（严格难过真版）。
4. **终结者奖励 $8**（普通 Boss $5）。

## 修复

- `Data.FINISHERS` 集合；`chooseBoss` 按 `ante % 8 == 0` 分池抽取（banned 过滤与去重保持）；
  `blindTarget` needle→1×/vessel→6×（wall 4× 不变）；`endRound` 终结者奖励 $8。
- 无丑牌挑战（jokerless 禁 3 终结者）下底注 8 池恰剩 瓶+铃——与真版 Wiki trivia 一致。
- **种子复现**：Boss 流取法从 28 池改为 23/5 分池——**Boss 选择序列相对 REF 为有意分歧**
  （真版对齐既定授权域）；非 Boss 流零影响；boss.txt 仅 needle 行手调（600→300），其余
  golden 场景直设 bossQueue 不受影响，全部通过。

## 测试

新增 `ShowdownSchedulingTest` ×5：300 种子底注 1 无终结者、23 常规全可达、快进至底注 8
必为终结者、needle 300/vessel 1800/wall 1200 倍率、终结者奖励 $8（heart 载体+handsLeft=1
隔离）。旧 vessel ×3 断言更新为 ×6。
`./gradlew test`：**433 全过**（0 失败）。

## 兼容性

行为变化：Boss 出现序列（分池）、瓶/针目标分、终结者奖励（真版对齐）；其余零变更。
0.4.x 直接替换 jar。
