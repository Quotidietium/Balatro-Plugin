# Balatro v0.4.15 — 全 20 挑战 mods + 全 24 标签完整 REF 比对（确认轮）

> 基线：Minecraft 1.21.11 / Paper API（Papo 服务端）。本轮做两项完整比对：
> 全 20 挑战 mods 与 REF data.js 逐字段 diff、全 24 标签定义/效果/计数器与 REF diff。
> **结论：100% 一致、无缺陷**；补 1 项全 20 挑战加载完整性回归测试。

## 审计确认（留档）

### 全 20 挑战 mods vs REF data.js（逐字段）
- **100% 一致**：每个挑战的每个 mod key 与值与 REF 完全匹配（omelette/city15/rich/knife/xray/
  madworld/luxury/nonperish/medusa/double/typecast/inflation/bram/fragile/monolith/blastoff/
  fivecard/golden/cruelty/jokerless）。
- `double` 的 `hands=-99` 在 applyOne default 有意忽略（handsSet=1 覆盖），与 REF 语义一致。
- 所有 mod 效果在引擎中正确消费（shopDiscount/minRewardMoney/rewardMult/smallBigRewardHalf/
  jokerTax/glassDouble/must5/noJokers/blindMult/handSize/handsSet/doubleBoss/allEternal/facesToStone/
  checkered/numbersToFaces/allStone/inflation/doubleInterest/freeReroll/noInterest 均追踪到消费点）。

### 全 24 标签 vs REF（定义 + 效果 + 计数器）
- Data.TAGS = 24 标签，key/name/desc 与 REF 完全一致。
- gainTagOne 全 24 效果正确：double（复制下一非 double 标签）、uncommon/rare/negative/foil/holo/poly
  （nextShop rarity/edition）、invest（Boss 击败 +$25）、voucher（nextShop extraVoucher 累加）、
  boss（rerollBoss）、standard/buffoon（立即开包）、charm/meteor（下商店塔罗/星球免费）、
  ethereal（幻灵包）、coupon（卡牌+包免费）、d6（重掷免费）、topup（2 随机普通小丑）、
  handy/garbage/speed（按计数器发钱）、orbital（随机牌型 +3 级）、economy（$1/$5 至多 $25）、
  juggle（下回合手牌 +3）。
- 依赖的计数器正确：statsHandsPlayed（playHand:656）/ statsDiscardsUnused（endRound:905）/ 
  statsBlindsSkipped（selectBlind:256）。

## 新增测试

`ChallengeModsAllLoadTest`：全 20 挑战逐一断言 ① ChallengeMods.exists(key)（键可加载，防
Data.CHALLENGES ↔ challengemods.txt 键不符致静默无 mod）；② createRun 不崩溃；③ 至少一个 mod 生效
（与全默认 Mods 比对）。填补现有 spot-check（仅 omelette）的覆盖空白。

## 测试

`./gradlew clean build`：**297 个测试全部通过**（62 个测试类，0 失败 0 错误）。

## 构建

`./gradlew build` → `build/libs/balatro-0.4.15.jar`（JDK 21，paper-api 1.21.11）。

## 兼容性

- paper-api 1.21.11（`compileOnly`），Vault softdepend 不变。
- 无代码逻辑/配置/存档/API 变化（仅新增测试）；0.4.x 直接替换 jar 即可。
