# Balatro v0.4.55 — R139：Boss 域表述-实现核对（vessel desc 漏改修复）

> 基线：Minecraft 1.21.11 / Paper API（Papo 服务端）。审计重启第三轮（R139）。

## 改动

1. **#69 紫罗兰之瓶 desc 漏改**：R126 已把代码对齐真版 6×（原 REF 3×，ShowdownSchedulingTest
   锁 300×6=1800），但 `Data.Boss.VESSEL` 的 desc 字符串漏改为「目标分 ×3」——该 desc 经
   命令「Boss 效果：」提示与全息简介直达玩家，属玩家可见的表述-实现不符。修正为 ×6。
   Boss desc 不在 golden data.txt 锁内（无 BOSS 行），此前无测试守护。

## 守门测试（+1，共 484 全过）

- `BossDescMultiplierConsistencyTest`：解析 desc 的「×N」与 blindTarget 实测倍率
  （被测 Boss 目标分 ÷ 基础分）比对——wall/vessel 双向锁定，desc 与代码任一侧漂移即失败。
  **变异验证**：desc 临时改回 ×3 → 测试失败 → 还原全过（非虚假锁定）。

## 审计范围（本轮）

28 Boss 效果分支在 P3–P15 计分管线重写后的落点-表述逐项核对（R132「重写使既有修复
落点漂移」教训的直接应用）：hook/ox/house/wall/wheel/arm/四花色/fish/psychic/serpent/
pillar/needle/tooth/flint/mark/acorn/bell/heart/water/manacle/eye/mouth/plant/leaf
全部落点正确（含 R129 紫蜡封被动弃牌触发、R130 onDiscard、R132 计分前块、R134 burnt
豁免四处后置修复仍在原位）；唯一缺陷即 #69。R126 其余三项（showdown 调度/needle 1×/
终结者 $8）的 desc 无倍率或奖励表述，无同类残留。

## 兼容性

单条玩家可见文案修正，零行为变化。0.4.x 直接替换 jar。
