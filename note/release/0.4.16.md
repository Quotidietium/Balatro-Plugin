# Balatro v0.4.16 — 全 13 补充包 + 消耗品计数完整 REF 比对（确认轮）

> 基线：Minecraft 1.21.11 / Paper API（Papo 服务端）。本轮完成全 13 补充包定义逐字段比对、
> 消耗品三类计数比对，并把全部静态表大小核验转为永久回归测试。**结论：100% REF 一致、无缺陷**。

## 审计确认（留档）

### 全 13 补充包 vs REF data.js（逐字段）
- **100% 一致**：每包的 key/type/name/size/choose/cost 与 REF 完全匹配（arcana×3 / celestial×3 /
  standard×3 / buffoon×3 / spectral×1）。
- Packs.open/pick/skip 逻辑（R5 验）+ 包定义（本轮验）均对齐。

### 消耗品计数 vs REF
- **Tarot=22 / Planet=12 / Spectral=18**——Java 与 REF 三项全等。
- 12 星球映射 13 牌型中的 12 个（high/pair/twopair/three/straight/flush/full/four/sflush/five/fhouse/ffive）；
  ROYAL（皇家同花顺）无专属星球——对齐 Balatro/REF（皇家共享同花顺的升级路径，无独立星球牌）。

### 静态数据表完整性（全表锁定）
至此全部静态表计数与内容均经 REF 比对：150 小丑 / 22 塔罗 / 12 星球 / 18 幻灵 / 28 Boss /
24 标签 / 20 挑战 / 32 券 / 15 牌组 / 13 牌型 / 13 补充包 / 8 赌注 / 4 版本 / 8 增强 / 4 蜡封。

## 新增测试

`DataCompletenessTest` ×2：锁定全部静态表大小（防意外删减——DataGoldenTest 仅验字段值，
删条目不会失败，本测试补「数量」维度）；并断言 JokerRegistry 覆盖全部 BasicJoker 枚举常量。

## 测试

`./gradlew clean build`：**299 个测试全部通过**（63 个测试类，0 失败 0 错误）。

## 构建

`./gradlew build` → `build/libs/balatro-0.4.16.jar`（JDK 21，paper-api 1.21.11）。

## 兼容性

- paper-api 1.21.11（`compileOnly`），Vault softdepend 不变。
- 无代码逻辑/配置/存档/API 变化（仅新增测试）；0.4.x 直接替换 jar 即可。
