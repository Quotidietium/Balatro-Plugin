# Balatro v0.4.45 — P7 性能优化：Bukkit 渲染/交互层评估

> 基线：Minecraft 1.21.11 / Paper API（Papo 服务端）。性能优化战役第 6 个改动轮（评估轮）。

## 改动

1. `RoundBoard.setScale`：常量缩放的 `Transformation` 缓存为静态单例（免每次更新每张牌
   new Transformation + 2×Vector3f）；渲染输出逐值相同。

## 评估结论

渲染层既有架构已是最优形态（实体池 + 原地差量更新 + 事件驱动无 tick + Interaction
原生命中盒 + 150ms 节流）；引擎侧优化（HandEval 3.66× / Suit.byIndex O(1) / EnumMap）
已自动继承到渲染热路径。更深层渲染改造无实机环境不可验证（稳定性红线），转实机清单
第 17 项（TPS 基线）后按数据决策。详见 note/report/perf/2026-08-16-P7-Bukkit层评估.md。

## 兼容性

458 测试全过。0.4.x 直接替换 jar。
